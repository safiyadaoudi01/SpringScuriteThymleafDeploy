# ===============================
# = DATA SOURCE
# ===============================
# Connection url for the database "boot"
spring.datasource.url = jdbc:mysql://viaduct.proxy.rlwy.net:31765/railway
# Username and password
spring.datasource.username = root
spring.datasource.password =DGecB-HD6BFE2A1BGa5fee5-dG--4e-6
server.servlet.session.serialization.ALLOW_JAVA_SERIALIZATION=false

# application.properties
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

server.port = 8088
# ===============================
# = JPA / HIBERNATE
# = JPA / HIBERNATE
# ===============================

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).

# Show or not log for each sql query
spring.jpa.show-sql = true
spring.jpa.properties.hibernate.format_sql=true
# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
spring.jpa.hibernate.ddl-auto = update

springdoc.swagger-ui.path=/swagger-ui
spring.security.user.password=password
# Configurations pour Gmail

spring.mail.host= smtp.gmail.com
spring.mail.port=587
spring.mail.username=hajarmachmoum546@gmail.com
spring.mail.password=ouat joes ohnj jipf


spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.thymeleaf.mode=DEBUG








